---
'@varveos/contracts': minor
---

목표·습관·기록 스키마 추가: `goalRow`(상태·지표 유형·목표값·수동 진행률)·`goalProgress`, `milestoneRow`, `habitRow`(빈도: 매일·요일·주 N회)·`habitCheckSet`·`habitStats`, `trackerRow`(number·scale·bool)·`recordRow`·`trackerSummary`, `paths.goals`·`habits`·`trackers`·`records`.
