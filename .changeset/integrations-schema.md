---
'@varveos/contracts': minor
---

연동 스키마 추가: connectorProvider, connectionRow/List, connectionSourceRow/List/Patch(direction·enabled·categoryId), integrationStartQuery/Response, syncQueued, disconnectQuery, providerInfo/List, `paths.integrations.*`, 에러 코드 integration_disabled(503)·provider_error(502).
